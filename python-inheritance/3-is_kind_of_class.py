#!/usr/bin/python3
"""Function to check if an object is an instance of a class"""


def is_instance_of_class(obj, a_class):
    """Function that returns True if the object is an instance of the specified class"""
    return isinstance(obj, a_class)
